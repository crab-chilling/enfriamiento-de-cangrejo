spring:
  application:
    name: UserService
  datasource:
    name: user-service
    url: jdbc:h2:mem:my_db
    driverClassName: org.h2.Driver
    username: sa
    password: sa
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect